# Listening Sessions

## Local development

Start the built-in PHP server from the project root:

```
php -S localhost:8000
```

Then open http://localhost:8000 in your browser.

The add-session password defaults to `admin` unless a `config.php` defining `PASSWORD` is present (see deploy workflow).
